//Création de la map
let initialZoom = 13;
// [lat,long]
let coordLille= [50.632971, 3.058585];
var map = L.map('show-event-map').setView(coordLille, initialZoom);

var myLayer = L.tileLayer('https://{s}.tile.openstreetmap.se/hydda/full/{z}/{x}/{y}.png').addTo(map);
