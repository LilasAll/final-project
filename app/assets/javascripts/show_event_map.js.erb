//Création de la map
let initialZoom = 14;
// On récupère les coordonnées de l'event :
let latitude = Number(gon.event.latitude) ;
let longitude = Number(gon.event.longitude) ;

var map = L.map('show-event-map').setView([latitude,longitude], initialZoom);

var myLayer = L.tileLayer('https://{s}.tile.openstreetmap.se/hydda/full/{z}/{x}/{y}.png').addTo(map);




var theMarker = L.circle([latitude,longitude], {
    color: 'red',
    fillColor: '#f03',
    fillOpacity: 0.5,
    radius: 400
		}).addTo(map);
		

