

     <div style="margin-top: 100px;" class="main main-raised">
        <div class="section section-basic">
          <div class=" container">
       

<div id="mail" class="">
    <h1 class="panel-heading">Conversations</h1>
    <div class="panel-body">
     <div class="item">
      <% @conversations.each do |conversation| %>
      <% if conversation.sender_id == current_user.id || conversation.recipient_id == current_user.id %>
       <% if conversation.sender_id == current_user.id %>
         <% recipient = User.find(conversation.recipient_id) %>
       <% else %>
         <% recipient = User.find(conversation.sender_id) %>
       <% end %>
       <h3><%= link_to recipient.pseudo, conversation_messages_path(conversation)%></h3>
      <% end %>
     <% end %>
     </div>
    </div>
   </div>




          </div>
        </div>
        <div class>
        </div>
      </div>

      

     <div style="margin-top: 100px;" class="main main-raised">
        <div class="section section-basic">
          <div class=" container">
       

  <div id="new" class="">
    <div class="panel-body">
     <h1 class="panel-heading">All Users:</h1>
       <% @users.each do |user| %>
        <% if user.id != current_user.id %>
         <div class="item"><h3>
      <%= link_to user.pseudo,
       conversations_path(sender_id: current_user.id, recipient_id: user.id), method: "post"%>
     </h3></div>
      <% end %>
     <% end %>
    </div>
  </div>




          </div>
        </div>
        <div class>
        </div>
      </div>


  
<div class="panel panel-default">
<div class="col-lg-6">
  

</div>
</div>
